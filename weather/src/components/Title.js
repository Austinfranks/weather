import React from "react";

// Extrac tthe property which than displays the error message from the return function
const Title = ({ error }) => (
  <div callName="titler">
    <p>Enter the name of the city to learnt he cities current weather</p>
  </div>
);

export default Title;
